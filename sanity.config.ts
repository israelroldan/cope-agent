/**
 * Sanity Studio Configuration
 *
 * Must be at project root (Sanity requirement).
 * Imports from src/sanity/ where the actual config lives.
 */
export { default } from './src/sanity/sanity.config.js';
